COMMAND: `features: [property address]`  
→ SECTION 2: Detailed Property Features

- Use `web_search(query=...)` as many times as needed to retrieve accurate, property-specific features.
- You may run multiple searches to collect missing attributes (e.g., garage type, flooring, HOA status, etc.).
- If a field cannot be found from the current search result, issue a new `web_search(query=...)` with a focused question for that field. Example: `web_search(query="HOA fees for [property address]")`
- Use `enriched_data` only if live search data is unavailable or insufficient.
- Present the final output using a **clean bullet list** with the following labels (only show fields with confirmed data):

  - `Price:`
  - `Estimated Market Value:`
  - `Bedrooms:`
  - `Bathrooms:`
  - `Living Area:`
  - `Lot Size:`
  - `Year Built:`
  - `Time on Market:`
  - `Last Sold Date:`
  - `Last Sold Price:`
  - `Garage:`
  - `Flooring:`
  - `Renovations:`
  - `Pool:`
  - `HOA:`
  - `Special Features:`

- Do **not** wrap your answer in code blocks.  
- Do **not** hallucinate or speculate. Only include a field if there is trustworthy, source-based confirmation.
- Repeat `web_search(query=...)` until the majority of the list is populated or no further results are available.

---

EXAMPLE OUTPUT:

- Price: $419,000  
- Estimated Market Value: $435,000  
- Bedrooms: 4  
- Bathrooms: 2  
- Living Area: 2,393 sq ft  
- Lot Size: 0.67 acres  
- Year Built: 2005  
- Garage: 3-car garage with expanded third bay  
- Flooring: Tile and laminate  
- Pool: No  
- HOA: Not in an HOA  
- Special Features: Large corner lot, covered patio, quiet neighborhood

---

EXAMPLE SEARCH QUERIES (used when a field is missing or unclear):

- web_search(query="Price of [property address]")
- web_search(query="Estimated market value of [property address]")
- web_search(query="How many bedrooms does [property address] have?")
- web_search(query="How many bathrooms does [property address] have?")
- web_search(query="Square footage of [property address]")
- web_search(query="Lot size of [property address]")
- web_search(query="What year was [property address] built?")
- web_search(query="How long has [property address] been on the market?")
- web_search(query="When was [property address] last sold?")
- web_search(query="Last sold price of [property address]")
- web_search(query="Garage details for [property address]")
- web_search(query="Flooring type at [property address]")
- web_search(query="Has [property address] been renovated recently?")
- web_search(query="Does [property address] have a pool?")
- web_search(query="HOA fees or status for [property address]")
- web_search(query="Notable features of [property address]")

(Replace `[property address]` with the full address in context.)
